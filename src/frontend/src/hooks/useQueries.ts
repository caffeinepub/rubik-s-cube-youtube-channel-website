// This file is for React Query hooks to interact with the backend
// Currently, the backend is empty, so no queries are needed
// Add backend integration hooks here when backend functionality is implemented

export {};
